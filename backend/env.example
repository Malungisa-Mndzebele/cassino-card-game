# Database Configuration
# For Fly.io PostgreSQL (set automatically by Fly.io)
# DATABASE_URL is provided by Fly.io environment variables

# For local PostgreSQL
# DATABASE_URL=postgresql://casino_user:casino_password@localhost:5432/casino_game

# For local SQLite (fallback for development)
# DATABASE_URL=sqlite:///./test_casino_game.db

# Application Configuration
PORT=8000
HOST=0.0.0.0

# CORS Origins (comma-separated)
# Set your frontend URL here for production
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,http://127.0.0.1:3001,https://your-frontend.fly.dev

# Logging
LOG_LEVEL=INFO
