name: Deprecated - Backend-only Deploy (Manual)

on:
  workflow_dispatch:

jobs:
  noop:
    name: Notice
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is deprecated
        run: echo "Use .github/workflows/ci.yml for full CI/CD (tests → build → deploy)."

